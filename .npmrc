# pnpm 配置文件
# 启用 shamefully-hoist 以提高兼容性
shamefully-hoist=true

# 启用严格的对等依赖项检查
strict-peer-dependencies=false

# 自动安装对等依赖项
auto-install-peers=true

# 启用 dedupe 以减少磁盘空间使用
dedupe-peer-dependents=true

# 启用缓存以加快后续安装
store-dir=~/.pnpm-store

# 允许构建脚本
enable-pre-post-scripts=true 